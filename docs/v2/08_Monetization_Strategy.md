# 08. 商业化与变现策略 (Monetization Strategy)

> **Document Purpose**: 明确项目的盈利模式，并在 V2 架构中预留对应的技术接口。
> **Strategy Source**: V1 `Business_Architecture_Analysis`

## 1. 核心哲学 (Philosophy)
*   **Open Source Core**: 代码开源，核心功能（排表、导出）永久免费。
*   **Monetized Service**: 变现的是“托管服务”、“身份象征”和“定制化劳动”。

---

## 2. 会员体系 (The Tier System)

我们将用户分为三个等级，V2 的 User Store 需要包含对应的权限字段。

### 🥉 Tier 1: 格子守护者 (Grid Guardian)
*   **价格**: ~¥6.00 / 月 (发电)
*   **权益**:
    *   **Credits**: 导出图片底部的“感谢名单”中出现名字。
    *   **Badge**: 社区/QQ群头衔。
*   **技术实现**: 纯纯的 `user_level` 字段标记，无实质功能差异。

### 🥈 Tier 2: 内测鉴赏家 (The Insider)
*   **价格**: ~¥18.00 / 月
*   **权益**:
    *   **Feature Voting**: 拥有对 Roadmap 的投票权。
    *   **Beta Access**: 提前使用测试版功能 (e.g., 视频导出, AI 辅助填表)。
*   **技术实现**: Feature Flags (特性开关) 系统需要检查此 Level。

### 🥇 Tier 3: 制作委员会 (The Production Committee)
*   **价格**: 高额单次付费 (¥68+) 或年费。
*   **权益**: **专属定制皮肤 (Premium Skins)**。
*   **场景**: "我是 MyGO 粉丝团团长，我集资定制一款 MyGO 专属主题，所有团员都能用。"

---

## 3. 技术实现要求：定制皮肤系统

Tier 3 的核心卖点是**皮肤**。这要求 V2 的渲染引擎 (`Canvas` 或 `DOM-to-Image`) 必须高度可配置，而不能硬编码。

### 3.1 Theme Config 接口
在 `src/v2/core/models/Theme.ts` 中定义：

```typescript
export interface ThemeConfig {
  id: string;
  name: string;
  isPremium: boolean; // 🔒 只有特定用户或公开授权由于可使用
  
  // 视觉配置
  colors: {
    background: string | 'transparent'; // 支持背景图
    textPrimary: string;
    textSecondary: string;
    accent: string;
    border: string;
  };
  
  // 资源资产 (Assets)
  assets: {
    backgroundImageUrl?: string; // 只有 Tier 3 定制才有的高清背景
    overlayFrameUrl?: string;    // 覆盖在格子上面的装饰框 (e.g., 赛博朋克边框)
    watermarkLogoUrl?: string;   // 赞助商/粉丝团 LOGO
  };
  
  // 字体配置
  fontFamily: string; // e.g., 'Ma Shan Zheng'
}
```

### 3.2 渲染流程变化
1.  **加载**: 如果检测到 Theme 包含外部资源 (背景图, 字体)，必须先预加载 (`Preload`)。
2.  **绘制**:
    *   先画背景 (`assets.backgroundImageUrl`)。
    *   画格子线。
    *   画内容。
    *   **最后**画前景装饰 (`assets.overlayFrameUrl`) 和赞助商 Logo (`assets.watermarkLogoUrl`)。

### 3.3 权限控制
*   虽然代码开源，但**高清素材资源**不一定开源。
*   后端 API `/api/v2/themes` 可以根据用户权限返回不同的 theme list。

---

## 4. 总结
V2 的架构必须支持 **"Theme as Data"** (主题即数据)。不要把样式写死在 CSS 类里，尤其是涉及到 Canvas 导出的部分，必须完全由 `ThemeConfig` 驱动。
